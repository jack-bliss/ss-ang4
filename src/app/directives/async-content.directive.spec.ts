import { AsyncContentDirective } from './async-content.directive';

describe('AsyncContentDirective', () => {
  it('should create an instance', () => {
    //const directive = new AsyncContentDirective();
    expect(true).toBeTruthy();
  });
});
